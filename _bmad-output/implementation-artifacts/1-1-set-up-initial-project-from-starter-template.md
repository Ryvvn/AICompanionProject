# Story 1.1: Set Up Initial Project from Starter Template

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As Ryan,
I want a local Python project foundation for Bananalyzer,
so that I can run and develop the companion from my workstation.

## Acceptance Criteria

1. **Given** the project has not yet been initialized
   **When** the developer initializes the app foundation
   **Then** the project uses Python 3.11+ with `uv` and a `src/bananalyzer/` package layout
   **And** dependencies include Typer, Rich, Textual, Pydantic, Pydantic Settings, psutil, pywin32, and httpx.

2. **Given** the project foundation exists
   **When** development dependencies are installed
   **Then** pytest and ruff are available through `uv run`
   **And** placeholder tests can run successfully.

3. **Given** Ryan opens the project
   **When** he inspects the source tree
   **Then** the architecture modules exist for CLI, config, diagnostics, events, foreground detection, mode controller, model router, persona, privacy, state machine, integrations, memory, UI, accountability, context, and utilities.

4. **Given** this is a personal local MVP
   **When** quality automation is configured
   **Then** local `uv run pytest` and `uv run ruff check .` commands are available
   **And** remote CI/CD is documented as optional and out of scope unless Ryan chooses to add it later.

## Tasks / Subtasks

- [x] Initialize UV Python app scaffold (AC: 1)
  - [x] Run `uv init bananalyzer --app --python 3.11` in the intended project folder.
  - [x] Ensure package path is `src/bananalyzer/` and entrypoint imports resolve.

- [x] Add required runtime and dev dependencies (AC: 1, 2)
  - [x] Runtime: `typer`, `rich`, `textual`, `pydantic`, `pydantic-settings`, `psutil`, `pywin32`, `httpx`.
  - [x] Dev: `pytest`, `ruff`.
  - [x] Confirm dependencies are recorded in `pyproject.toml` and lockfile is generated by `uv`.

- [x] Create architecture-aligned module skeleton (AC: 3)
  - [x] Add root package files:
    - [x] `src/bananalyzer/__init__.py`
    - [x] `src/bananalyzer/__main__.py`
    - [x] `src/bananalyzer/cli.py`
    - [x] `src/bananalyzer/config.py`
    - [x] `src/bananalyzer/constants.py`
    - [x] `src/bananalyzer/diagnostics.py`
    - [x] `src/bananalyzer/events.py`
    - [x] `src/bananalyzer/foreground.py`
    - [x] `src/bananalyzer/mode_controller.py`
    - [x] `src/bananalyzer/model_router.py`
    - [x] `src/bananalyzer/persona.py`
    - [x] `src/bananalyzer/privacy.py`
    - [x] `src/bananalyzer/state_machine.py`
  - [x] Add subpackages and module placeholders:
    - [x] `src/bananalyzer/accountability/{__init__.py, banana_debt.py, interventions.py, signals.py}`
    - [x] `src/bananalyzer/context/{__init__.py, code_context.py, screen_context.py, context_builder.py}`
    - [x] `src/bananalyzer/integrations/{__init__.py, base.py, ollama.py, screenpipe.py, mcp.py, stt.py, tts.py}`
    - [x] `src/bananalyzer/memory/{__init__.py, store.py, summarizer.py, retrieval.py}`
    - [x] `src/bananalyzer/ui/{__init__.py, dashboard.py}`
    - [x] `src/bananalyzer/utils/{__init__.py, files.py, time.py}`

- [x] Add baseline tests and quality command checks (AC: 2, 4)
  - [x] Create `tests/` tree with placeholder tests sufficient for import/smoke validation.
  - [x] Ensure `uv run pytest` passes.
  - [x] Ensure `uv run ruff check .` passes.

- [x] Keep MVP CI/CD scope correct (AC: 4)
  - [x] Do not add mandatory remote CI pipelines.
  - [x] Add brief local note that CI/CD is optional and post-MVP.

## Dev Notes

### Story Foundation

- This is the first implementation story in Epic 1 and sets the baseline project scaffold.
- Scope is intentionally **foundation-only**: project init, package layout, modules, dependencies, and local quality commands.
- Do **not** implement behavior-heavy logic yet (state transitions, integrations, routing intelligence, memory behavior).

### Technical Requirements

- Runtime: Python 3.11+.
- Package manager / runner: `uv`.
- Required runtime deps: Typer, Rich, Textual, Pydantic, Pydantic Settings, psutil, pywin32, httpx.
- Required dev deps: pytest, ruff.
- Operation mode: local-first, no cloud dependency required.

### Architecture Compliance Guardrails

- Respect boundaries from the architecture:
  - CLI command definitions in `cli.py`.
  - Runtime loop coordination in `mode_controller.py`.
  - State transition logic in `state_machine.py`.
  - External tool communication isolated to `integrations/*.py`.
- Canonical persisted state values for future stories must be only:
  - `coding`, `gaming`, `doomscrolling`, `companion`, `fallback`.
- Naming conventions:
  - Python and JSON/YAML fields use `snake_case`.
- Avoid premature implementations that bypass adapter boundaries.

### Existing UPDATE Files Analysis (Must Preserve)

1. `mode_controller.py`
   - **Current state:** Prototype mode detector maps app names to `CODING`, `GAMING`, `DUO`; pauses active job on coding exit; includes local demo sequence.
   - **What this story changes:** Nothing in-place is required for this story.
   - **What must be preserved:** Keep prototype behavior intact unless explicitly requested; do not delete or mutate as part of scaffold setup.

2. `job_tracker.py`
   - **Current state:** Event-sourced local job tracker with JSONL persistence, CLI operations, status/priority handling, focus-time accounting.
   - **What this story changes:** Nothing in-place is required for this story.
   - **What must be preserved:** Keep existing tracker operational and untouched during scaffold work.

### File Structure Requirements

- Create new architecture-aligned implementation under `src/bananalyzer/`.
- Keep root-level prototype files (`mode_controller.py`, `job_tracker.py`) unchanged.
- Do not reorganize unrelated existing folders/assets in repo root.

### Testing Requirements

Minimum done checks for this story:

1. `uv run pytest` succeeds with scaffold tests.
2. `uv run ruff check .` succeeds.
3. `src/` package imports for `bananalyzer` work from uv execution context.
4. Tests do not require live integrations or network calls.

### Previous Story Intelligence

- Not applicable (this is Story 1.1).

### Git Intelligence Summary

- Not applicable in this environment (project is not currently a git repository).

### Latest Tech Information

- Web lookup was attempted for package/version freshness, but no reliable result payload was returned in-session.
- Use latest stable package releases at implementation time and keep Python target at 3.11+ per architecture.

### Project Context Reference

- Persistent fact pattern `**/project-context.md` was searched and no file was found.

### Risks / Anti-Patterns to Avoid

- Do not implement full runtime logic in this story.
- Do not hardcode integration calls in orchestrator modules.
- Do not add cloud/auth/installer/tray concerns (explicitly out of MVP scope).
- Do not introduce non-canonical state names in persisted state paths.

### References

- Story definition: `_bmad-output/planning-artifacts/epics.md` (Epic 1, Story 1.1)
- Architecture starter + module boundaries: `_bmad-output/planning-artifacts/architecture.md`
- Functional/NFR baseline: `_bmad-output/planning-artifacts/prd.md`
- Existing prototype files reviewed: `mode_controller.py`, `job_tracker.py`

## Story Completion Status

- Status set to: `review`
- Completion note: Scaffolded the uv project, added baseline dependencies, created architecture package placeholder files, configured ruff to exclude prototyping files, and verified simple test and lint checks pass.

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

- _To be filled by dev agent_

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.

### File List

- `_bmad-output/implementation-artifacts/1-1-set-up-initial-project-from-starter-template.md`
